StackScope is an open-source web technology inspection tool that analyzes publicly available HTML and HTTP headers to infer the technologies used by a website.

Detection is heuristic-based and intentionally limited to avoid invasive or browser-level inspection.